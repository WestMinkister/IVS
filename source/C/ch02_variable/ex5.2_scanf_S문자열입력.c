#include <stdio.h>

//int main(void) {
//	char gender, name[100]; // name은 [100]개짜리 값들의 첫 주소가 있는 변수
//	printf("성별은? ");
//	scanf_s("%c", &gender, 1);
//	printf("이름은? ");
//	scanf_s("%s", name, (unsigned)sizeof(name)); // 변수 크기에 음수가 와선 안 되므로 양수로 바꾼다.(당연하긴 하지만..)
//	printf("입력한 성별은 %c\n이름은 %s입니다.\n", gender, name);
//}